<%= MultiJson.dump({
  auth_token: token,
  callback_url: callback_url,
  line: {
    nid: nid,
    carrier_name: operator_name,
    carrier_id: carrier_code,
    plan_id: plan_code,
    service_details: {
      msid: msid,
      msl: msl,
      csa: csa,
      zip: zip
      }
    }
  }) %>
