<%= MultiJson.dump({
  auth_token: token,
  callback_url: callback_url,
  line: {
    nid: nid,
    carrier_name: carrier_name,
    carrier_id: carrier_id,
    plan_id: plan_id,
    service_details: {
      msid: msid,
      msl: msl,
      csa: csa,
      zip: zip
      }
    }
  }) %>
